# Examples

Shot examples live in the main repository: https://github.com/didley/EspressoScript/tree/main/examples

- `examples/shotLint/` — TypeScript projects using shot-lint rules and utils
- `examples/both/` — examples with both a ShotScript (`.shot`) and shot-lint (`.ts`) version side by side
